<section class='main'>
  <% @images.each do |i| %>
    <%= link_to image_path(i.id) do %>
      <div class='thumb'>
         <%= image_tag i.avatar.url(:medium) %>
        <div class='txthover' >
          <%= i.title %>
          <%= i.votes.length %>
        </div>
      </div>
    <% end %>
  <% end %>
</section>


